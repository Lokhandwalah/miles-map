{
  "permissions": {
    "defaultMode": "auto",
    "allow": [
      "Bash(flutter --version)",
      "WebSearch",
      "WebFetch",
      "Read(**)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(sudo *)"
    ]
  }
}
